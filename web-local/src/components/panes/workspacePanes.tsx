import { useEffect, useMemo, useRef, useState, type ChangeEvent, type ReactNode } from "react";
import { Bot, Check, Clock3, Code2, Copy, FileText, Folder, FolderOpen, History, LibraryBig, Loader2, MessageSquare, Package, Palette, PencilLine, Plug, RefreshCw, ShieldCheck, Sparkles, Trash2, Upload } from "lucide-react";
import { api, apiForm, errorText } from "../../lib/api";
import { formatChatListTime, formatItemCount } from "../../lib/format";
import { workspaceIdentityFor, workspaceIdentityStyle, type WorkspaceIdentity } from "../../lib/workspace-identity";
import type { AgentCatalog, AgentModel, AgentStatus, ConversationSummary, TreeEntry, WorkspaceCheckpoint, WorkspaceCustomizationMap, WorkspaceSummary } from "../../types";
import { EmptyInline, WorkspaceIconGlyph } from "../chrome/common";
import { FileTypeIcon } from "../tree/FileTree";
import { requestConfirm } from "../../ui/feedback";
import { WorkspaceRenameEditor } from "./workspaceChrome";

export function SpacesPane({ workspace, workspaces, identities, onSwitch, onCreate, onOpenFolder, onCustomize, onRename, onRemove }: {
  workspace: WorkspaceSummary;
  workspaces: WorkspaceSummary[];
  identities: WorkspaceCustomizationMap;
  onSwitch: (workspace: WorkspaceSummary) => void;
  onCreate: () => void;
  onOpenFolder: () => void;
  onCustomize: (workspace: WorkspaceSummary) => void;
  onRename: (workspace: WorkspaceSummary, name: string) => Promise<void>;
  onRemove?: (workspace: WorkspaceSummary) => void;
}) {
  const [renamingId, setRenamingId] = useState<string | null>(null);
  return <div className="workspace-pane-content workspaces-pane">
    <section className="workspace-start-section"><span className="workspace-start-kicker">SPACES</span><h1>Where does this work live?</h1><p>Use an existing folder or create a clean one. Either way, the folder remains an ordinary folder you control.</p><div className="workspace-start-actions">
      <button className="workspace-start-action local" type="button" onClick={onOpenFolder}><span><FolderOpen size={21} /></span><span className="workspace-start-tip"><strong>Existing folder</strong><small>Turn it into a Space</small></span></button>
      <button className="workspace-start-action chat" type="button" onClick={onCreate}><span><Sparkles size={21} /></span><span className="workspace-start-tip"><strong>New Space</strong><small>Start with a clean folder</small></span></button>
    </div></section>
    <div className="pane-category-stack workspace-category-stack"><section className="workspace-pane-section"><div className="pane-category-heading"><div><span className="pane-category-kicker">YOUR SPACES</span><strong>{formatItemCount(workspaces.length, "Space")}</strong></div></div><div className="workspace-switcher">
      {workspaces.map((item) => { const identity = workspaceIdentityFor(item, identities); return <div className={item.id === workspace.id ? "workspace-card-shell active" : "workspace-card-shell"} key={item.id} style={workspaceIdentityStyle(identity)}><div className="workspace-card-row"><button className={item.id === workspace.id ? "workspace-tab workspace-card-main active" : "workspace-tab workspace-card-main"} type="button" onClick={() => onSwitch(item)}><span className="workspace-tab-icon workspace-identity-icon"><WorkspaceIconGlyph icon={identity.Icon} size={16} /></span><span className="workspace-tab-copy"><strong>{item.name}</strong><span>{item.location.providerHint === "google-drive" ? "Google Drive" : item.location.storage === "linked" ? "Linked folder" : "Managed folder"}</span></span>{item.id === workspace.id ? <span className="active-dot" aria-label="Active Space"><Check size={11} /></span> : null}</button><span className="workspace-card-actions"><button className="workspace-card-rename" type="button" onClick={() => setRenamingId((current) => current === item.id ? null : item.id)} aria-label={`Rename ${item.name}`} title="Rename Space"><PencilLine size={15} /></button><button className="workspace-card-customize" type="button" onClick={() => onCustomize(item)} aria-label={`Customize ${item.name}`} title="Customize Space"><Palette size={15} /></button>{onRemove ? <button className="workspace-card-delete" type="button" onClick={() => onRemove(item)} aria-label={`Remove ${item.name}`} title="Remove Space"><Trash2 size={15} /></button> : null}</span></div><WorkspaceRenameEditor open={renamingId === item.id} workspace={item} onRenameWorkspace={onRename} onClose={() => setRenamingId(null)} /></div>; })}
    </div></section></div>
  </div>;
}

export function ChatsPane({ workspace, workspaces, conversations, customizations, activeConversationId, onOpen, onNew, onRename }: {
  workspace: WorkspaceSummary;
  workspaces: WorkspaceSummary[];
  conversations: Record<string, ConversationSummary[]>;
  customizations: WorkspaceCustomizationMap;
  activeConversationId?: string | null;
  onOpen: (workspace: WorkspaceSummary, conversation: ConversationSummary) => void;
  onNew: (workspace: WorkspaceSummary) => void;
  onRename: (workspace: WorkspaceSummary, conversation: ConversationSummary, event: React.MouseEvent) => void;
}) {
  const [query, setQuery] = useState("");
  const normalized = query.trim().toLocaleLowerCase();
  return <div className="workspace-pane-content chats-pane"><div className="file-tree-toolbar"><label className="file-tree-search"><MessageSquare size={15} /><input type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Chats" aria-label="Search Chats" /></label></div><div className="chat-workspace-groups">
    {[workspace, ...workspaces.filter((item) => item.id !== workspace.id)].map((item) => { const list = (conversations[item.id] ?? []).filter((chat) => !normalized || chat.title.toLocaleLowerCase().includes(normalized)); const identity = workspaceIdentityFor(item, customizations); return <section className="chat-workspace-group" key={item.id} style={workspaceIdentityStyle(identity)}><div className="chat-workspace-heading"><span><WorkspaceIconGlyph icon={identity.Icon} size={15} /><strong>{item.name}</strong></span><button className="minimal-icon-button" type="button" onClick={() => onNew(item)} aria-label={`New Chat in ${item.name}`} title="New Chat"><MessageSquare size={15} /></button></div><div className={item.id === workspace.id ? "chat-workspace-list chat-workspace-list-current" : "chat-workspace-list"}>{list.map((chat) => <button className={chat.id === activeConversationId ? "chat-workspace-row active" : "chat-workspace-row"} type="button" key={chat.id} onClick={() => onOpen(item, chat)} onContextMenu={(event) => { event.preventDefault(); onRename(item, chat, event); }}><span className="chat-workspace-row-title">{chat.title}</span><span className="chat-workspace-row-time">{formatChatListTime(chat.updatedAt)}</span></button>)}{!list.length ? <span className="chat-workspace-empty">{normalized ? "No matching Chats" : "No Chats yet"}</span> : null}</div></section>; })}
  </div></div>;
}

export function LibraryPane({ workspace, fixtureTree, onError }: { workspace: WorkspaceSummary; fixtureTree?: TreeEntry[]; onError: (message: string | null) => void }) {
  const [tree, setTree] = useState<TreeEntry[]>(fixtureTree ?? []);
  const [selected, setSelected] = useState<string | null>(null);
  const [query, setQuery] = useState("");
  const [busy, setBusy] = useState(false);
  const [notice, setNotice] = useState("");
  const uploadRef = useRef<HTMLInputElement>(null);
  const selectedEntry = selected ? findTreeEntry(tree, selected) : null;
  useEffect(() => { if (!fixtureTree) void load(); }, [fixtureTree]);
  async function load() { try { setTree((await api<{ tree: TreeEntry[] }>("/api/resources/tree")).tree); } catch (caught) { onError(errorText(caught)); } }
  async function upload(event: ChangeEvent<HTMLInputElement>) { const files = Array.from(event.target.files ?? []); event.target.value = ""; if (!files.length || fixtureTree) return; const form = new FormData(); form.set("targetFolderPath", selectedEntry?.kind === "folder" ? selectedEntry.path : ""); form.set("relativePaths", JSON.stringify(files.map((file) => file.webkitRelativePath || file.name))); files.forEach((file) => form.append("files", file, file.name)); setBusy(true); try { await apiForm("/api/resources/upload", form); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  async function createFolder() { if (fixtureTree) return; const name = window.prompt("Folder name"); if (!name?.trim()) return; setBusy(true); try { await api("/api/resources/folders", { method: "POST", body: { parentPath: selectedEntry?.kind === "folder" ? selectedEntry.path : "", name: name.trim() } }); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  async function copyToSpace() { if (!selected) return; if (fixtureTree) { setNotice(`Preview: ${selectedEntry?.name ?? "item"} would be copied to ${workspace.name}.`); return; } setBusy(true); setNotice(""); try { const result = await api<{ copied: string[] }>("/api/resources/copy-to-workspace", { method: "POST", body: { workspaceId: workspace.id, paths: [selected], targetFolder: "From Library" } }); setNotice(`Added ${result.copied[0] ?? selectedEntry?.name ?? "item"} to ${workspace.name}.`); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  const visible = query.trim() ? filterTree(tree, query.trim().toLocaleLowerCase()) : tree;
  return <div className="workspace-pane-content library-pane"><div className="file-tree-toolbar"><label className="file-tree-search"><LibraryBig size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Library" /></label><button className="minimal-icon-button" type="button" disabled={busy || Boolean(fixtureTree)} onClick={() => uploadRef.current?.click()} title="Add files"><Upload size={15} /></button><button className="minimal-icon-button" type="button" disabled={busy || Boolean(fixtureTree)} onClick={() => void createFolder()} title="New folder"><Folder size={15} /></button><input hidden ref={uploadRef} type="file" multiple onChange={(event) => void upload(event)} /></div><div className="library-split"><div className="library-tree"><LibraryTree entries={visible} selected={selected} onSelect={setSelected} /></div><div className="library-detail">{selectedEntry ? <div className="resource-selection"><div className="card-icon large">{selectedEntry.kind === "folder" ? <Folder size={26} /> : <FileTypeIcon path={selectedEntry.path} />}</div><p className="eyebrow">LIBRARY ITEM</p><h2>{selectedEntry.name}</h2><span>{selectedEntry.path}</span><button className="primary-button" type="button" disabled={busy} onClick={() => void copyToSpace()}>{busy ? <Loader2 className="spin" size={16} /> : <Copy size={16} />}Add to {workspace.name}</button>{notice ? <p className="copy-status"><Check size={15} />{notice}</p> : null}<p>Workspace makes an independent copy under <strong>From Library</strong>. It is not automatically included in a Chat.</p></div> : <EmptyState icon={<LibraryBig size={30} />} title="Your reusable Library" detail="Keep templates, examples, and reference files here so they can be copied into any Space." />}</div></div></div>;
}

export function HistoryPane({ workspace, fixtureItems, refreshRequest = 0, onOpen, onError }: { workspace: WorkspaceSummary; fixtureItems?: WorkspaceCheckpoint[]; refreshRequest?: number; onOpen?: (item?: WorkspaceCheckpoint) => void; onError: (message: string | null) => void }) {
  const [items, setItems] = useState<WorkspaceCheckpoint[]>(fixtureItems ?? []);
  const [busy, setBusy] = useState(false);
  useEffect(() => { if (!fixtureItems) void load(); }, [workspace.id, fixtureItems]);
  useEffect(() => { if (!fixtureItems && refreshRequest > 0) void load(); }, [refreshRequest]);
  async function load() { try { setItems((await api<{ checkpoints: WorkspaceCheckpoint[] }>(`/api/workspaces/${workspace.id}/history/checkpoints`)).checkpoints); } catch (caught) { onError(errorText(caught)); } }
  async function savePoint() { if (fixtureItems) return; setBusy(true); try { await api(`/api/workspaces/${workspace.id}/history/checkpoints`, { method: "POST", body: { label: "Manual restore point" } }); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  async function restore(item: WorkspaceCheckpoint) { if (fixtureItems) return; const confirmed = await requestConfirm({ title: `Restore ${workspace.name}?`, body: `Return the Space to ${formatDate(item.createdAt)}. Current files will be replaced by that restore point.`, confirmLabel: "Restore", tone: "danger" }); if (!confirmed) return; setBusy(true); try { await api(`/api/workspaces/${workspace.id}/history/checkpoints/${item.checkpointId}/restore`, { method: "POST", body: {} }); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  return <div className="workspace-pane-content history-pane"><div className="history-pane-actions"><button className="primary-button compact" type="button" onClick={() => void savePoint()} disabled={busy || Boolean(fixtureItems)}>{busy ? <Loader2 className="spin" size={15} /> : <Clock3 size={15} />}Save restore point</button></div><div className="history-list">{items.map((item) => <article key={item.checkpointId} onDoubleClick={() => onOpen?.(item)}><div className="history-icon"><History size={17} /></div><div><strong>{item.label || item.reason}</strong><span>{formatDate(item.createdAt)} · {item.fileCount} files</span></div><button className="secondary-button compact" type="button" disabled={busy || Boolean(fixtureItems)} onClick={() => void restore(item)}>Restore</button></article>)}{!items.length ? <EmptyState icon={<History size={30} />} title="No restore points yet" detail="Workspace creates restore points before important file changes. You can make one manually too." /> : null}</div></div>;
}

export function AssistantSetupPane({ workspace, status, fixtureMode = false, onConfigured }: { workspace: WorkspaceSummary; status: AgentStatus; fixtureMode?: boolean; onConfigured: (status: AgentStatus) => void }) {
  const [models, setModels] = useState<AgentModel[]>([]); const [provider, setProvider] = useState(status.provider ?? "openrouter"); const [model, setModel] = useState(status.model ?? ""); const [apiKey, setApiKey] = useState(""); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null);
  useEffect(() => { if (fixtureMode) { setModels([{ provider: "openrouter", id: "anthropic/claude-sonnet-4", name: "Claude Sonnet", authConfigured: true, oauthSupported: false }]); setProvider("openrouter"); setModel("anthropic/claude-sonnet-4"); setLoading(false); return; } void api<{ models: AgentModel[] }>(`/api/agent/models?workspaceId=${encodeURIComponent(workspace.id)}`).then((result) => { setModels(result.models); const first = result.models.find((item) => item.provider === provider) ?? result.models.find((item) => item.provider === "openrouter") ?? result.models[0]; if (first) { setProvider(first.provider); setModel((current) => result.models.some((item) => item.provider === first.provider && item.id === current) ? current : first.id); } }).catch((caught) => setError(errorText(caught))).finally(() => setLoading(false)); }, [fixtureMode, workspace.id]);
  const providers = unique(models.map((item) => item.provider)).sort(); const providerModels = models.filter((item) => item.provider === provider); const oauthSupported = providerModels.some((item) => item.oauthSupported); const authConfigured = providerModels.some((item) => item.authConfigured);
  useEffect(() => { if (!providerModels.some((item) => item.id === model)) setModel(providerModels[0]?.id ?? ""); }, [provider]);
  async function configure(oauth = false) { if (fixtureMode) { onConfigured({ ...status, configured: true, provider, model }); return; } setSaving(true); setError(null); try { const result = await api<{ status: AgentStatus }>(oauth ? "/api/agent/oauth" : "/api/agent/configure", { method: "POST", body: { workspaceId: workspace.id, provider, model, ...(oauth ? {} : { apiKey: apiKey.trim() || undefined }) } }); setModels((current) => current.map((item) => item.provider === provider ? { ...item, authConfigured: true } : item)); setApiKey(""); onConfigured(result.status); } catch (caught) { setError(errorText(caught)); } finally { setSaving(false); } }
  return <div className="workspace-pane-content assistant-pane"><div className="assistant-setup-card"><div className="setup-intro"><Bot size={24} /><div><strong>Your Assistant, your provider</strong><p>Workspace uses Pi with its built-in tools, Skills, and Extensions.</p></div></div>{loading ? <LoadingRow label="Loading Pi models" /> : <div className="setup-grid">{error ? <div className="inline-error">{error}</div> : null}<label>Provider<select value={provider} onChange={(event) => { setProvider(event.target.value); setModel(""); }}>{providers.map((item) => <option key={item}>{item}</option>)}</select></label><label>Model<select value={model} onChange={(event) => setModel(event.target.value)}>{providerModels.map((item) => <option value={item.id} key={item.id}>{item.name || item.id}</option>)}</select></label><label>API key <span>stored securely on this computer</span><input type="password" value={apiKey} onChange={(event) => setApiKey(event.target.value)} placeholder={authConfigured ? "Saved credential available" : "Paste a key"} autoComplete="off" /></label><button className="primary-button" type="button" onClick={() => void configure()} disabled={saving || !model || (!authConfigured && !apiKey.trim())}>{saving ? <Loader2 className="spin" size={16} /> : <Check size={16} />}Save setup</button>{oauthSupported ? <button className="secondary-button" type="button" onClick={() => void configure(true)} disabled={saving}>Connect with OAuth</button> : null}<p className="security-note"><ShieldCheck size={14} />Capabilities stored inside a Space load only after you trust that Space.</p></div>}</div></div>;
}

export function AssistantCapabilityPane({ workspace, mode, status, fixtureMode = false, onOpenSetup, onError }: { workspace: WorkspaceSummary; mode: "skills" | "extensions"; status: AgentStatus; fixtureMode?: boolean; onOpenSetup: () => void; onError: (message: string | null) => void }) {
  const [catalog, setCatalog] = useState<AgentCatalog | null>(null); const [source, setSource] = useState(""); const [scope, setScope] = useState<"global" | "project">("global"); const [busy, setBusy] = useState(false); const importRef = useRef<HTMLInputElement>(null);
  useEffect(() => { if (fixtureMode) { setCatalog(fixtureCatalog()); return; } void load(); }, [fixtureMode, workspace.id]); async function load() { if (fixtureMode) return; try { setCatalog(await api<AgentCatalog>(`/api/workspaces/${workspace.id}/agent/catalog`)); } catch (caught) { onError(errorText(caught)); } }
  async function trust() { if (fixtureMode) { setCatalog((current) => current ? { ...current, projectTrusted: !current.projectTrusted } : current); return; } try { setCatalog((await api<{ catalog: AgentCatalog }>(`/api/workspaces/${workspace.id}/agent/trust`, { method: "POST", body: { trusted: !catalog?.projectTrusted } })).catalog); } catch (caught) { onError(errorText(caught)); } }
  async function install() { if (!source.trim() || fixtureMode) return; setBusy(true); try { await api("/api/agent/packages/install", { method: "POST", body: { workspaceId: workspace.id, source: source.trim(), scope } }); setSource(""); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  async function importSkills(event: ChangeEvent<HTMLInputElement>) { const files = Array.from(event.target.files ?? []); event.target.value = ""; if (!files.length || fixtureMode) return; const form = new FormData(); form.set("workspaceId", workspace.id); form.set("scope", scope); files.forEach((file) => form.append("files", file, file.name)); setBusy(true); try { await apiForm("/api/agent/skills/import", form); await load(); } catch (caught) { onError(errorText(caught)); } finally { setBusy(false); } }
  return <div className="workspace-pane-content assistant-pane">{!status.configured ? <div className="trust-banner"><Bot size={18} /><div><strong>Assistant not set up yet</strong><span>You can organize capabilities now and choose a provider when you are ready.</span></div><button className="secondary-button compact" type="button" onClick={onOpenSetup}>Open Setup</button></div> : null}{catalog ? <div className="trust-banner"><ShieldCheck size={18} /><div><strong>{catalog.projectTrusted ? "This Space is trusted" : "This Space is not trusted"}</strong><span>{catalog.projectTrusted ? "Capabilities stored inside it may load." : "Personal capabilities still work. Trust the Space to load local capabilities."}</span></div><button className="secondary-button compact" type="button" onClick={() => void trust()}>{catalog.projectTrusted ? "Remove trust" : "Trust Space"}</button></div> : <LoadingRow label="Loading capabilities" />}<section className="install-panel"><div className="scope-toggle"><button className={scope === "global" ? "active" : ""} type="button" onClick={() => setScope("global")}>Personal</button><button className={scope === "project" ? "active" : ""} type="button" onClick={() => setScope("project")}>This Space</button></div>{mode === "skills" ? <><button className="primary-button compact" type="button" disabled={busy} onClick={() => importRef.current?.click()}><Upload size={15} />Import Skill or pack</button><input hidden ref={importRef} type="file" accept=".zip,.skill,.md" multiple onChange={(event) => void importSkills(event)} /></> : null}<label className="package-input"><Package size={15} /><input value={source} onChange={(event) => setSource(event.target.value)} placeholder="npm package, git URL, or local path" /><button type="button" disabled={busy || !source.trim()} onClick={() => void install()}>{busy ? <Loader2 className="spin" size={15} /> : "Install"}</button></label></section>{catalog?.diagnostics.length ? <div className="diagnostics">{catalog.diagnostics.map((item, index) => <span className={item.type} key={`${item.message}-${index}`}>{item.message}</span>)}</div> : null}{mode === "skills" ? <div className="card-grid">{catalog?.skills.map((skill) => <CapabilityCard key={`${skill.source}:${skill.name}`} icon={<Sparkles size={18} />} title={skill.name} detail={skill.description} meta={`${skill.source} · ${skill.path}`} enabled={skill.enabled} />)}{catalog && !catalog.skills.length ? <EmptyState icon={<Sparkles size={30} />} title="No Skills loaded" detail="Import an Agent Skill or compatible Skill pack, or add one to a standard Pi location." /> : null}</div> : <><div className="card-grid">{catalog?.extensions.map((extension) => <CapabilityCard key={`${extension.source}:${extension.id}`} icon={<Plug size={18} />} title={extension.name} detail={`${extension.tools.length} tools · ${extension.commands.length} commands`} meta={`${extension.source} · ${extension.path}`} enabled={extension.enabled} />)}{catalog && !catalog.extensions.length ? <EmptyState icon={<Plug size={30} />} title="No Extensions loaded" detail="Install a Pi package containing an Extension, or add one to a trusted .pi/extensions folder." /> : null}</div>{catalog?.tools.length ? <details className="tool-details"><summary><Code2 size={15} />Available tools ({catalog.tools.length})</summary><div className="tool-list">{catalog.tools.map((tool) => <span key={tool.name}><strong>{tool.name}</strong>{tool.description}<small>{tool.source}</small></span>)}</div></details> : null}</>}</div>;
}

function LibraryTree({ entries, selected, onSelect, level = 0 }: { entries: TreeEntry[]; selected: string | null; onSelect: (path: string) => void; level?: number }) { return <div className="file-tree">{entries.map((entry) => <div className="file-tree-item" key={entry.path}><button className={selected === entry.path ? "file-row selected" : "file-row"} style={{ paddingLeft: 12 + level * 16 }} type="button" onClick={() => onSelect(entry.path)}>{entry.kind === "folder" ? <Folder size={15} /> : <FileTypeIcon path={entry.path} />}<span className="file-name">{entry.name}</span></button>{entry.children?.length ? <LibraryTree entries={entry.children} selected={selected} onSelect={onSelect} level={level + 1} /> : null}</div>)}</div>; }
function CapabilityCard({ icon, title, detail, meta, enabled }: { icon: ReactNode; title: string; detail: string; meta: string; enabled: boolean }) { return <article className="resource-card"><div className="card-icon">{icon}</div><div><strong>{title}</strong><p>{detail}</p><small>{meta}</small></div><span className={enabled ? "enabled-badge" : "disabled-badge"}>{enabled ? "Enabled" : "Disabled"}</span></article>; }
function EmptyState({ icon, title, detail }: { icon: ReactNode; title: string; detail: string }) { return <div className="empty-state"><div>{icon}</div><h2>{title}</h2><p>{detail}</p></div>; }
function LoadingRow({ label }: { label: string }) { return <div className="loading-row"><Loader2 className="spin" size={15} />{label}</div>; }
function unique<T>(items: T[]) { return [...new Set(items)]; }
function formatDate(value: string) { return new Date(value).toLocaleString(undefined, { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }); }
function findTreeEntry(entries: TreeEntry[], path: string): TreeEntry | null { for (const entry of entries) { if (entry.path === path) return entry; const child = entry.children ? findTreeEntry(entry.children, path) : null; if (child) return child; } return null; }
function filterTree(entries: TreeEntry[], query: string): TreeEntry[] { return entries.flatMap((entry) => { const children = entry.children ? filterTree(entry.children, query) : []; return entry.name.toLocaleLowerCase().includes(query) || children.length ? [{ ...entry, children }] : []; }); }
function fixtureCatalog(): AgentCatalog { return { projectTrusted: true, diagnostics: [], packages: [], skills: [{ name: "Trip planner", description: "Turns bookings and preferences into a practical itinerary.", path: "skills/trip-planner/SKILL.md", source: "personal", enabled: true }], extensions: [{ id: "calendar", name: "Calendar helper", path: ".pi/extensions/calendar.ts", source: "project", enabled: true, commands: ["calendar"], tools: ["read_calendar"] }], tools: [{ name: "read", description: "Read files in the current Space", source: "Pi", active: true }, { name: "write", description: "Create and update files", source: "Pi", active: true }] }; }
